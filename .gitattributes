# Finder launches this file directly; keep its POSIX shebang valid when the
# repository is prepared or published from Windows.
launch_synpo.command text eol=lf

# Preserve native line endings for the Windows entry points.
*.bat text eol=crlf
*.ps1 text eol=crlf
